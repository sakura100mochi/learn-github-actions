name: norminette

on:
  push:
    branches: branch1
  pull_request:
    branches: branch1

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: bash norm.sh 
